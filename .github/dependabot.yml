version: 2
updates:
  # Configuration for Python pip
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "daily"
    labels:
      - "pip dependencies"
    versioning-strategy: lockfile-only
    allow:
      # Allow both direct and indirect updates for all packages
      - dependency-type: "all"